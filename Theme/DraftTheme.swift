//  DraftTheme.swift
//  Drift

import SwiftUI

enum DraftTheme {
    static let background = Color(hex: "#070707")
    static let text       = Color(hex: "#F4EFE8")
    static let secondary  = Color(hex: "#cf9763")
    static let surface    = Color(hex: "#111111")
    static let divider    = Color.white.opacity(0.06)
}
